# Скопируйте этот файл в .env и вставьте туда СВОЙ ключ.
# Ключ НИКОГДА не публикуется и не попадает в браузер посетителей сайта.

# Ключ от Anthropic (console.anthropic.com) если LLM_PROVIDER = "anthropic"
# или от OpenAI (platform.openai.com) если LLM_PROVIDER = "openai"
API_KEY=вставьте_сюда_свой_ключ
